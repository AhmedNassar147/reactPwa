import React from 'react';
import styled from 'styled-components';

const Wrapper = props => <WrapperPage {...props} />

const WrapperPage = styled.section`
  padding: 4em;
  background: papayawhip;
`;

export default Wrapper;
